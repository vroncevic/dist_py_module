name: dist_py_module triage label
on:
  issues:
    types: [opened]
jobs:
  applyTriageLabel:
    name: dist_py_module triage label
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: dist_py_module triage label
        uses: actions/github@v1.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          args: label triage
