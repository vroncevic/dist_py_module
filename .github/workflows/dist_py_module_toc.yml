name: dist_py_module_toc
on:
  push:
    branches: [ master ]
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: dist_py_module_toc
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v2
