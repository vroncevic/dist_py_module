name: dist_py_module_package toc generator
on:
  push:
    branches: [ master ]
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: dist_py_module_package toc generator
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v2
